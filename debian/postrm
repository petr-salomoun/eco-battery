#!/bin/bash
set -e

case "$1" in
    purge)
        # Remove config directory on purge
        rm -rf /home/*/.config/eco-battery 2>/dev/null || true
        ;;
    remove|upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)
        ;;
    *)
        echo "postrm called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

#DEBHELPER#

exit 0